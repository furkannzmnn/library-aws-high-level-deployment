pipelines:
  default:
    - step:
        name: Testleri koşuyoruz
        image: maven:3.6.3-jdk-8
        caches:
          - maven
        script:
          - mvn -s mvn-config-bitbucket.xml verify


definitions:
  services:
    mysql:
      image: mysql:5.7
      variables:
        MYSQL_DATABASE: 'pipelines'
        MYSQL_ROOT_PASSWORD: 'test_user_password'